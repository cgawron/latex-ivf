image: marvinmenzerath/latex

build:
  stage: build
  artifacts:
    paths:
      - tex/lehrbrief-vorlage.pdf
  script:
    - cd tex
    - mkdir ~/.fonts
    - cp -r font/* ~/.fonts
    - luaotfload-tool -u
    - latexmk -pdflatex=lualatex -pdf lehrbrief-vorlage.tex